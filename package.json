{
  "name": "boot-code",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "workspaces": ["apps/web"],
  "scripts": {
    "dev": "node ./scripts/dev.js",
    "build": "powershell ./scripts/build.ps1",
    "preview": "cd build/ && dotnet ./Api.dll",
    "format": "biome format --write .",
    "lint": "biome lint --write .",
    "check": "biome check --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.5",
    "@types/react-syntax-highlighter": "^15.5.13",
    "concurrently": "^9.2.1"
  }
}
